apiVersion: v1
data:
  DB_ADAPTER: postgresql
  DB_DB: {{ .Values.db.database }}
  DB_HOST: {{ .Values.db.host }}
  {{- if .Values.sentry.dsn }}
  SENTRY_DSN: {{ .Values.sentry.dsn }}
  {{- end }}
  {{- if .Values.tracing.zipkin_host }}
  ZIPKIN_REPORT_HOST: {{ .Values.tracing.zipkin_host }}
  {{- end }}
kind: ConfigMap
metadata:
  name: config
