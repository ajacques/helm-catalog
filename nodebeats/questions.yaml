questions:

- variable: config.output.elasticsearch.hosts
  default: http://elasticsearch:9200
  type: string
  required: true

- variable: config.output.elasticsearch.index
  default: packetbeat-%{+YYYY.MM}
  type: string
  required: true

- variable: kibana.hostname
  type: string
  required: true
  label: Kibana Hostname
