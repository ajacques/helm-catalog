apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: postfix
spec:
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: deployment-mail-postfix
  template:
    metadata:
      labels:
        workload.user.cattle.io/workloadselector: deployment-mail-postfix
    spec:
      containers:
      - name: opendkim
        args:
        - /usr/sbin/opendkim
        - -f
        image: ajacques/kube-mail:latest
        imagePullPolicy: Always
        volumeMounts:
        - mountPath: /etc/opendkim.conf
          name: dkimconfig
          readOnly: true
          subPath: opendkim.conf
      hostname: mail
      volumes:
      - configMap:
          defaultMode: 292
          name: opendkimconf
          optional: false
        name: dkimconfig
